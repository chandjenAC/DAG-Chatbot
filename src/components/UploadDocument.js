import React from "react";

const UploadDocument = props => {
  return (
    <div style={{ color: "black", fontSize: "12px", width: "100%" }}>
      Hi from Upload component
    </div>
  );
};

export default UploadDocument;
